## run backend
```cd backend
mvn spring-boot:run```

### if bove cmd didn't work, run cmd below
```mvn spring-boot:run "-Dspring-boot.run.arguments=--server.port=8081"```

## run frontend
```cd frontend
npm run dev```
